import { AppDisableIf } from './app-disable-if';

describe('AppDisableIf', () => {
  it('should create an instance', () => {
    const directive = new AppDisableIf();
    expect(directive).toBeTruthy();
  });
});
