import { PhoneNumberExtensionPipe } from './phone-number-extension-pipe';

describe('PhoneNumberExtensionPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneNumberExtensionPipe();
    expect(pipe).toBeTruthy();
  });
});
