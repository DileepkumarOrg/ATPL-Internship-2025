import { CustomImpurePipePipe } from './custom-impure-pipe-pipe';

describe('CustomImpurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomImpurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
