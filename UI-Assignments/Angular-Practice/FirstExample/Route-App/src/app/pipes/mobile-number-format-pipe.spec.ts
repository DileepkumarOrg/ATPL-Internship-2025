import { MobileNumberFormatPipe } from './mobile-number-format-pipe';

describe('MobileNumberFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MobileNumberFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
