import { CustomPurePipePipe } from './custom-pure-pipe-pipe';

describe('CustomPurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomPurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
